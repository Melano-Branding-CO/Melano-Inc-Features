# Melano Inc Features

Minimal Vite starter app for the Melano Inc feature workspace.

## Commands

```sh
npm install
npm run dev
npm run build
npm run start
npm test
```

The repository root proxies these commands into this directory, so `npm run build` from `/workspace` builds this app.
